// lib/progress/models/doubt_stats_model.dart
import 'package:flutter/material.dart';

class DoubtStatsModelScreen extends StatelessWidget {
  const DoubtStatsModelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Doubt Stats'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
