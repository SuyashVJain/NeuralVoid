// lib/progress/models/quiz_stats_model.dart
import 'package:flutter/material.dart';

class QuizStatsModelScreen extends StatelessWidget {
  const QuizStatsModelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Quiz Stats'),
      ),
      body: const Center(
        child: Text('Coming Soon'),
      ),
    );
  }
}
